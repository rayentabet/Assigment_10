# Evaluation datasets

The active System A golden dataset is `v2/cases.jsonl`. Its schema and
ground-truth policy are documented in `v2/README.md`.

`v1/` is archived and intentionally excluded from current runs because its
purchase expectations predate the DigiKey/AP2 Component Manager.
