# Agent System A - LangGraph supervisor, specialists, and FastAPI boundary.
# Kept in sync with pyproject.toml, which is what local development installs
# in editable mode; the container installs from this file so the service is
# independently buildable without the packaging metadata.
#
# pandas and streamlit are deliberately absent: they are imported only by
# evaluation/dashboard.py, which runs on the host, never inside this service.
# Leaving them out keeps pyarrow/numpy/altair out of the image, which matters
# because the pip install has to fit in Docker's memory budget.

ap2 @ git+https://github.com/google-agentic-commerce/AP2.git@e1ea56db72a6385bce3e5c1112b3a56ce60acb43
a2a-sdk>=1,<2
build123d>=0.9
faster-whisper>=1.2,<2
aiosqlite>=0.20,<1
fastapi>=0.115,<1
fastmcp>=2,<4
httpx>=0.27,<1
langchain>=0.3
langchain-google-genai>=2
langchain-groq>=0.3
langchain-mcp-adapters>=0.1
langchain-openai>=0.3
langgraph>=0.4
langgraph-checkpoint-sqlite>=3.0.1,<4
nemoguardrails>=0.21,<1
pillow>=10,<14
pydantic>=2.10
pydantic-settings>=2.7
python-dotenv>=1.0
uvicorn[standard]>=0.34,<1
